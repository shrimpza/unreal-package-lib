dist: xenial
language: java

jdk:
  - openjdk11

cache:
  directories:
    - /home/travis/.ant/

jobs:
  include:
    - stage: test
      script: ant test
    - stage: publish artefacts
      if: branch = master
      script: ant publish
